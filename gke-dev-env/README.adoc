= GKE DEV Environment

== Pre-requisites
1. Read the `README.adoc` and Execute `tf apply` in the parent `nonproduction` project
2. Replace the value `firebase_admin_service_account` in the  with the right service account ID as generated by the `firebase` module
    - Look for `Firebase service account` value at https://console.firebase.google.com/u/1/project/place-2-meet-dev/settings/serviceaccounts/adminsdk
    - This value is currently not supported as an attribute that can be exported via terraform outputs
[source,hcl]
----
include::./terraform/main.tf[lines=144..147]
----
